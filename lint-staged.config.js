// lint-staged.config.js
// Конфигурация для lint-staged
module.exports = {
  // Запускать TypeScript проверку на всех .ts и .tsx файлах
  "*.{ts,tsx}": ["tsc --noEmit"],
  // Запускать юнит-тесты
  "*.{ts,tsx}": ["npm run test:unit -- --run --filter="], // Этот вариант может не сработать идеально, так как тесты запускаются для всех файлов, а не только для измененных. Но для демонстрации подходит.
  // Альтернатива: запускать общий тестовый скрипт, который сам определит, что тестировать
  "*.{ts,tsx}": () => "npm run test:unit -- --run",
};