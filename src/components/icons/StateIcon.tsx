import type { FC } from 'react'

/**
 * Значки состояний: пусто, нечего показать, ждём снимок.
 *
 * ЗАЧЕМ ОТДЕЛЬНО ОТ CategoryIcon. Тот отвечает на вопрос «что это за
 * категория» и ключуется значениями из catalog.ts; камера и коробка
 * категориями не являются, и подмешивать их туда значит сделать словарь
 * категорий свалкой глифов. Две роли — два файла, идиома одна.
 *
 * ЗАЧЕМ ВООБЩЕ. В шапке CategoryIcon записано прямым текстом: «эмодзи
 * сегодня стоят ещё на витрине, на странице вещи и в форме выкладки.
 * Заменить их там — отдельная работа по четырём экранам». Это она.
 * На 19.09 в пустых состояниях жили 🔨 (витрина), 📦 («Мои вещи») и 📸
 * (выкладка) — полноцветные объёмные эмодзи посреди монохромного
 * продукта, где всё остальное рисуется одной обводкой. Особенно заметно
 * на телефоне: цветная картинка в 52 px — самое яркое пятно экрана,
 * ярче единственного действия.
 *
 * Идиома та же, что у CategoryIcon: 24×24, одна обводка 1.6, currentColor.
 * Цвет задаёт вызывающий — пустое состояние не должно кричать.
 */
const PATHS: Record<string, JSX.Element> = {
  // Ключ и разводной ключ: «инструментов нет».
  tool: (
    <>
      <path d="M18.4 3.6a4.6 4.6 0 0 0-6.1 6.1L4 18l2 2 8.3-8.3a4.6 4.6 0 0 0 6.1-6.1l-2.7 2.7-2.1-2.1z" />
    </>
  ),
  // Закрытая коробка: «здесь пока ничего не лежит».
  box: (
    <>
      <path d="M3 8l9-4 9 4-9 4-9-4z" />
      <path d="M3 8v8l9 4 9-4V8" />
      <path d="M12 12v8" />
    </>
  ),
  // Камера: «ждём снимок».
  camera: (
    <>
      <path d="M3 8.5h3.2l1.6-2.3h8.4l1.6 2.3H21v10H3v-10z" />
      <circle cx="12" cy="13" r="3.4" />
    </>
  ),
}

type Props = { name: keyof typeof PATHS | string; size?: number; className?: string }

const StateIcon: FC<Props> = ({ name, size = 48, className }) => (
  <svg
    width={size}
    height={size}
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth={1.6}
    strokeLinecap="round"
    strokeLinejoin="round"
    className={className}
    aria-hidden="true"
    focusable="false"
  >
    {PATHS[name] ?? PATHS.box}
  </svg>
)

export default StateIcon
